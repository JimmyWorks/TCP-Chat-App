#ifndef _CHATAPP_ERRORS_H
#define _CHATAPP_ERRORS_H

enum Error_Codes
{
   SUCCESS,
   CLI_ERROR,
   FILE_ACCESS_ERROR,
   FILE_PARSE_ERROR,
   SOCK_CREATE_FAILURE,
   SOCK_BIND_FAILURE,
   SOCK_LISTEN_FAILURE,
   INVALID_OPCODE,
   MESSAGE_ERROR
};

enum Op_Codes
{
   HEARTBEAT,
   CONN_REQ,
   CONN_TERM,
   CHAT_REQ,
   CHAT_TERM,
   CHAT_MESSAGE,
   INVALID_OP
};

enum Response_Codes
{
   ACK,
   NACK
};

#endif
